# 📅2026-04-23

## 初始化命令表

```cpp
    std::map<std::string, std::function<int(const std::string&)>> cmd_obj_api_table_;
```

- `std::map`：使用关联容器，实现红黑树，存储键值对
- 键是一个字符串
- 值是一个函数，`std::function`内部包装的是函数的结构；作用是声明一种特殊的函数类型


```cpp
    cmd_obj_api_table_[HAIER_Y_CLEAN_MODE]           = std::bind(&TaskControl::Cmd_YMop, &TASK_CTL_INS, std::placeholders::_1);
```

**使用命令表的意义：**

✅ 统一接口：所有函数通过 std::function<int(const std::string&)> 统一调用
✅ 动态路由：通过键值查找自动分发到对应函数
✅ 避免冗余：不需要大量重复的if-else分支
✅ 易于维护：新增/修改命令只需要操作映射表


- `std::bind(...)` 本身不执行被绑定的函数。它做的事是：生成一个“可调用对象”（binder 对象），真正执行发生在你“调用 bind 返回对象”的那一刻，也就是对它使用 `()` 时

```cpp
auto f = std::bind(&DevStatus::StatusForceReport, this, std::placeholders::_1);
StdStringMsg msg;
f(msg);  // 这一行才会调用 this->StatusForceReport(msg)
```

哪些类型可以作为 `std::function<R(Args...)>` 的值？

只要满足一句话：能用 `()` 调用，并且调用形式能匹配 `R(Args...)`（返回值可转换成 `R`，参数可用 `Args...` 传进去），就可以。

常见可装入 `std::function` 的类型：

- 普通函数指针
    
    - `void foo(int);`
    - `std::function<void(int)> f = foo;`
- 无捕获 lambda / 有捕获 lambda
    
    - `[](int x){...}` 或 `[this](const Msg& m){...}`
    - 捕获 lambda 不能隐式转函数指针，但可以进 `std::function`
- 函数对象（仿函数）
    
    - 自定义 `struct F { void operator()(int) const; };`
- `std::bind` 的返回对象
    
    - 你用的就是这种
- 成员函数指针（注意：不能“直接”放进去就能调用，通常要配合绑定对象）
    
    - `&DevStatus::StatusForceReport` 单独拿出来不够，还缺对象实例
    - 典型做法：用 `bind` 或 lambda 把 `this` 绑上，再放进 `std::function`
- `std::mem_fn` 返回的可调用对象（同样可塞进 `std::function`）
    
    - `std::mem_fn(&DevStatus::StatusForceReport)` 生成一个可调用包装，但调用时仍需要对象参数



## 执行函数

```cpp
int HaierAdapter::HaierWriteHandle(const std::string &cmd, const std::string &cmd_value) {
    std::lock_guard<std::mutex> locker(write_mutex_);
    LOGD("HaierWriteHandle %s", cmd.c_str());
    auto iter = cmd_obj_api_table_.find(cmd);
    if (iter != cmd_obj_api_table_.end()) {
        return iter->second(cmd_value);
    } else {
        LOGD("HaierWriteHandle cmd:%s not found in cmd_obj_api_table", cmd.c_str());
    }
    return -1;
}
```

- 传入命令名称与参数，验证之后进行执行

- cmd_obj_api_table_.find(cmd) 如果在键值对列表中找到了“对应的键”，就会反馈对应的键值的地址，通过这个地址，使用first和second就可以分别对应访问键和值


## 设备写操作的回调函数

- 接收海尔云端下发的设备控制指令，执行对应函数
- 返回结果到云端


```cpp
uhsd_s32 HaierAdapter::HaierWriteCallback(uhsd_devHandle devHandle, uhsd_s32 req_sn, const uhsd_char *property_name, const uhsd_char *property_value, const uhsd_char *traceId) {
    // 实现回调逻辑
    // 1. 使用 devHandle 来标识设备
    // 2. 记录 req_sn，用于响应
    // 3. 根据 property_name 和 property_value 进行属性设置
    // 4. 使用 traceId 进行链路跟踪
    // 返回状态码，例如 0 表示成功，非 0 表示失败

    LOGD("-----------HaierWriteCallback--------------");
    std::cout << "Device Handle: " << devHandle << std::endl;
    std::cout << "Request Serial Number: " << req_sn << std::endl;
    std::cout << "Property Name: " << (property_name ? property_name : "NULL") << std::endl;
    std::cout << "Property Value: " << (property_value ? property_value : "NULL") << std::endl;
    std::cout << "Trace ID: " << (traceId ? traceId : "NULL") << std::endl;

    int result = HAIER_ADPATER.HaierWriteHandle(std::string(property_name), std::string(property_value));
    LOGD("-----------HaierWriteCallback--------------:%d", result);
    return uhsd_dev_write_resp(devHandle, req_sn, result, 0, traceId);
}
```

uhsd_s32 HaierWriteCallback(
    uhsd_devHandle devHandle,           // 设备唯一标识符，区分不同设备
    uhsd_s32 req_sn,                    // 请求序列号，用于请求-响应匹配
    const uhsd_char *property_name,     // 命令名称，如"workCommand"、"powerLevel"
    const uhsd_char *property_value,    // 命令参数，如"start"、"3"
    const uhsd_char *traceId            // 链路追踪ID，用于日志追踪
)


## 云端数据反馈函数回调的注册

```cpp
    // 设置【写】设备 通知回调，开发者只有设置该回调，才能收到设备写属性通知
    res = uhsd_dev_set_write_cb(HaierWriteCallback);
    LOGW("uhsd_dev_set_write_cb res : %d", res);
```


# 📅2026-04-25

## C++中的容器
### 容器的定义

C++ STL（标准模板库）提供的**模板类**，用于**存储同类型数据集合**，内置了**增、删、查、遍历、排序**等现成方法。

### 容器的分类

1. 序列容器（按顺序存，像列表 / 数组）

按**存放顺序**排列数据：

- `vector`：**动态数组**（最常用，变长数组）
- `list`：双向链表
- `deque`：双向队列
- `array`：固定大小数组（STL 版）

2. 关联容器（按键查找，字典结构）

**key-value 键值对**，自动排序 / 哈希，适合查找：

- `map`：有序键值对（红黑树）
- `unordered_map`：**哈希键值对**（查找超快）
- `set`：唯一元素集合
- `unordered_set`：哈希集合

👉 你代码里的 `cmd_obj_api_table_.find()`

**`cmd_obj_api_table_` 就是 `map/unordered_map` 这类「关联容器」**

3. 容器适配器（包装出来的特殊容器）

基于上面容器封装，限制功能，实现特定逻辑：

- `stack`：栈（后进先出）
- `queue`：队列（先进先出）
- `priority_queue`：优先队列

### 容器的作用

- 原生数组 `int a[]` 缺点：
    
    - 大小固定、不能动态扩容
    - 增删麻烦、无查找方法
    
- STL 容器优点：
    - **模板化**：任意类型都能装（int、string、函数、对象）
    - **自带方法**：`find()`、`size()`、`clear()`、`push_back()`
    - **自动管理内存**、动态扩容
    - 迭代器统一遍历（你刚才看的 `iter` 就是容器配套工具）

## 采集-组包-上报

```cpp
std::map<int, std::function<int()>> cmd_map_table_;

void DevStatus::InitWorkCmdTable() {
    cmd_map_table_[DUST_COLLECTION]     = std::bind(&DevStatus::Cmd_StartDustCollection, this);
    cmd_map_table_[START_SWEEP]         = std::bind(&DevStatus::Cmd_StartTotalClean, this);
    cmd_map_table_[PAUSE_SWEEP]         = std::bind(&DevStatus::Cmd_SwitchPause, this);
    cmd_map_table_[CONTINUE_SWEEP]      = std::bind(&DevStatus::Cmd_ContinueClean, this);
    cmd_map_table_[RETURN_CHARGE]       = std::bind(&DevStatus::Cmd_StartCharge, this);
    cmd_map_table_[PAUSE_RETURN_CHARGE] = std::bind(&DevStatus::Cmd_PauseCharge, this);
    cmd_map_table_[LOCALIZE_ROBOT]      = std::bind(&DevStatus::Cmd_Seek, this);
    cmd_map_table_[FORWARD]             = std::bind(&DevStatus::Cmd_DirectionForward, this);
    cmd_map_table_[BACKWARD]            = std::bind(&DevStatus::Cmd_DirectionBack, this);
    cmd_map_table_[LEFT_TURN]           = std::bind(&DevStatus::Cmd_DirectionTurnLeft, this);
    cmd_map_table_[RIGHT_TURN]          = std::bind(&DevStatus::Cmd_DirectionTurnRight, this);
    cmd_map_table_[MANUAL_MODE_STOP]    = std::bind(&DevStatus::Cmd_DirectionStop, this);
    cmd_map_table_[EXPLORE_MAP]         = std::bind(&DevStatus::Cmd_ExploreMap, this);
    cmd_map_table_[EDGE_SWEEP]          = std::bind(&DevStatus::Cmd_EdgeMode, this);
    cmd_map_table_[RESET_FACTORY]       = std::bind(&DevStatus::Cmd_ResetFactory, this);
    cmd_map_table_[CLEAR_MAP]           = std::bind(&DevStatus::Cmd_ResetMap, this);
    cmd_map_table_[IDLE_MODE]           = std::bind(&DevStatus::Cmd_StopClean, this);
}
```

- 容器内部的数据类型，可以根据需要进行更改

```cpp
int DevStatus::Init() {
    InitWorkCmdTable();
    // 回充中状态（包含充电中、基站工作和暂停）
    property_switch_charge_converter_ = {
            {EVENT_WORK_STATUS_BACK_CHARGE, true},       
            {EVENT_WORK_STATUS_BASE_STATION, true},
            {EVENT_WORK_STATUS_BACK_CHARGE_PAUSE, false}, 
            {EVENT_WORK_STATUS_BASE_STATION_PAUSE, false},
            {EVENT_WORK_STATUS_CHARGING, false},          
            {EVENT_WORK_STATUS_FULL_CHARGING, false},
            {EVENT_WORK_STATUS_COLLECT_DUST, false},      
            {EVENT_WORK_STATUS_WASH_MOP, false}};

    staus_thread_.reset(new std::thread(std::bind(&DevStatus::DevStatusLoop, this)));
    EventWorkerInnerPtr worker_ptr(new EventWorker<const StdStringMsg &>(std::bind(&DevStatus::StatusForceReport, this, std::placeholders::_1)));
    IOT_SDK->Subscriber(TOPIC_SWEEP_DEV_STATUS, std::move(worker_ptr));
 
    return 0;
}
```


### 堆上对象与栈上对象

 1. **`new` = 创建对象 + 返回** **对象指针**

```cpp
new std::thread(...)
```
- 在 ** 堆（heap）** 上创建一个 `std::thread` 对象
- **返回值是：`std::thread*` 线程对象指针**
- 所以必须用**指针**来接收：
```cpp
std::thread* t = new std::thread(...);
```

2. **不带 `new` = 创建栈上对象 + 返回** **对象实体（值）**

```cpp
std::thread t( std::bind(...) );
```
- 在 ** 栈（stack）** 上创建对象
- **返回的是对象实体，不是指针**
- 不能用指针接收
- 生命周期到作用域结束就**自动销毁**

### 线程的相关知识点

```cpp
std::thread(std::bind(&DevStatus::DevStatusLoop, this))
```
- `std::thread` 构造函数一执行，线程立刻启动

```cpp
staus_thread_.reset
```
- 使用智能指针`status_thread_`的`reset`方法，后续可以直接使用`status_thread_`管理线程指针对象

等价为下面两种写法：
```cpp
staus_thread_ = std::make_shared<std::thread>(&DevStatus::DevStatusLoop, this);
```

```cpp
staus_thread_ = std::make_shared<std::thread>([this] { DevStatusLoop(); });
```


### 智能指针与普通指针的区别

> **智能指针**：
> 
> 是一个**包装类对象**，
> 
> 1. 拥有**自身方法**（`.` 调用：reset 等）
> 2. 重载`->`/`*`，**能像普通指针一样操作目标**
> 3. **自带自动内存回收**，安全

> **普通指针**：
> 
> 是**纯地址**，
> 
> 4. 无自身方法，无 `.`
> 5. 只能`->/*`操作目标
> 6. 不管理内存，需要手动 delete

```cpp
std::thread* ptr = new std::thread(...);

ptr->join(); // 指针只能用 ->
```

```cpp
unique_ptr<std::thread> ptr(new std::thread(...));

ptr.reset();   // 对象用 . 调用自己的方法
ptr->join();   // -> 是“解引用”，访问内部的线程
```





# 📅2026-04-26

## 智能指针管理模板类实例

```cpp
EventWorkerInnerPtr worker_ptr(new EventWorker<const StdStringMsg &>(std::bind(&DevStatus::StatusForceReport, this, std::placeholders::_1)));
```

`std::bind` 结果：是一个对象（内部保存了成员函数指针 + `this` + 占位符规则），将来被调用时才执行真正的成员函数



## 成员初始化列表

```cpp
class T {
  int a_;
  const int b_;
  int& c_;
  std::function<int(int)> f_;

public:
  T(int a, int b, int& c, std::function<int(int)> f)
    : a_(a)              // 用参数 a 初始化成员 a_
    , b_(b)              // 用参数 b 初始化 const 成员 b_
    , c_(c)              // 用引用 c 绑定到成员 c_
    , f_(std::move(f))   // 用 f 的右值来初始化/移动 f_
  {
      // 构造函数体
  }
};
```

- 类名+括号，首先想到构造函数


## 移动语义-右值引用

```cpp
template<typename MsgType>
class EventWorker : public EventWorkerBase
{
 private:
   typedef std::function<int32_t(const MsgType&)> SubFunc;
   SubFunc subscriber_func_;
 public:
   EventWorker(SubFunc func) : subscriber_func_(std::move(func)) {};
   int32_t Call(const MsgType& in_msg) {
        return subscriber_func_(in_msg);
   };
};
```

- 有名字、你能对它取址（大多数情况） 的东西，通常是左值（lvalue）。
- 临时对象、字面量、返回的非引用临时值 通常是纯右值（prvalue），常常表现为“没名字的临时结果”。


## 回调-订阅机制

```cpp
int DevStatus::Init() {
    InitWorkCmdTable();
    // 回充中状态（包含充电中、基站工作和暂停）
    property_switch_charge_converter_ = {
            {EVENT_WORK_STATUS_BACK_CHARGE, true},       
            {EVENT_WORK_STATUS_BASE_STATION, true},
            {EVENT_WORK_STATUS_BACK_CHARGE_PAUSE, false}, 
            {EVENT_WORK_STATUS_BASE_STATION_PAUSE, false},
            {EVENT_WORK_STATUS_CHARGING, false},          
            {EVENT_WORK_STATUS_FULL_CHARGING, false},
            {EVENT_WORK_STATUS_COLLECT_DUST, false},      
            {EVENT_WORK_STATUS_WASH_MOP, false}};

    staus_thread_.reset(new std::thread(std::bind(&DevStatus::DevStatusLoop, this)));
    EventWorkerInnerPtr worker_ptr(new EventWorker<const StdStringMsg &>(std::bind(&DevStatus::StatusForceReport, this, std::placeholders::_1)));
    IOT_SDK->Subscriber(TOPIC_SWEEP_DEV_STATUS, std::move(worker_ptr));
 
    return 0;
}
```

- 传入一个"可操作的对象"作为回调函数，后续会使用这个初始化的操作句柄给这个回调函数传入参数再实际调用

- 以 `TOPIC_SWEEP_DEV_STATUS` 作为事件/主题名（`const char*`）
- 把封装好的 `EventWorker`（基类指针 `EventWorkerBase*` 形式）注册进 SDK 的订阅表里保存起来
- 之后当该 event 有消息/事件需要处理时，SDK 会取出对应的 `EventWorker` 去调用其 `Call(...)`，从而最终触发你 `std::function` 里保存的回调

# 📅2026-04-27

## 线程告警上报



# 📅2026-04-28

## 线程循环函数

```cpp
void DevStatus::DevStatusLoop() {
    while (true) {
        bool  b_flag = false;
        request_signal_.PopSignal(b_flag, 300);
        UpdateDevAllStatus(b_flag);
    }
}
```

**其中调用了如下函数：**

```cpp
int32_t PopSignal(T& signal_out, int32_t timeout = -1) {
    // 查看是否有存货
    queue_mutex_.lock();
    if (event_queue_.size() > 0) {
        signal_out = event_queue_.front();
        event_queue_.pop();
        is_set_flag_ = false;
        queue_mutex_.unlock();
        return 1;
    }
    queue_mutex_.unlock();

    // 无存货，等新货
    bool signal_status = false;
    std::unique_lock<std::mutex> lck(cond_mutex_);
    if (timeout == -1) {
        // 无超时时间，永久等待
        condition_var_.wait(lck);
    } else {
        if(!is_set_flag_.load()) {
            // 等待新的消息,直到超时
            // 注: 只有当lanbada 表达式中的返回值为true时,wait才会提前退出;
            // 陷阱：当在进入等待状态前，通知发生，会导致通知丢失了，即条件变量仅在接收方处于等待状态时才发送通知。
            // std::condition_variable存在两个缺陷：虚假唤醒 和 唤醒丢失
            // 为解决以上两个问题，采取如下方案：每间隔100ms或收到信号时 查看下是否收到了信号，若收到了则退出，若没收到则继续wait_for，直到timeout
            int32_t once_timeout     = 0;
            int32_t once_timeout_max = 100;
            while (timeout > 0) {
                if (timeout > once_timeout_max) {
                    timeout      = timeout - once_timeout_max;
                    once_timeout = once_timeout_max;
                } else {
                    once_timeout = timeout;
                    timeout      = 0;
                }

                signal_status = condition_var_.wait_for(lck, std::chrono::milliseconds(once_timeout),
                                                        [&] { return is_set_flag_ == true; });
                
                if (signal_status) {
                    if (is_set_flag_.load() || event_queue_.size() > 0) {
                        break;
                    }
                }
                
                // debug log by zzx
                // printf("222 PopSignal ignore wait_for. is_set_flag:%d event_queue:%d signal_status:%d once_timeout:%d\n", is_set_flag_.load(), event_queue_.size(), signal_status, once_timeout);
            }

        } else {
            signal_status = true;
        }
    }

    // 来货了
    if (signal_status == true) {
        queue_mutex_.lock();
        if (event_queue_.size() > 0) {
            signal_out = event_queue_.front();
            event_queue_.pop();
            queue_mutex_.unlock();
            is_set_flag_ = false;
            return 1;
        }
        queue_mutex_.unlock();
    }

    // debug log by zzx
    // printf("333 PopSignal ignore wait_for. is_set_flag:%d event_queue:%d signal_status:%d timeout:%d\n", is_set_flag_.load(), event_queue_.size(), signal_status, timeout);
    is_set_flag_ = false;
    return -1;
}
```

- 队列中如果有数据，就直接将数据给出去，并解锁


- 调用函数时传入的参数，决定后续进入的分支
- 调用 PopSignal 时，传入的 timeout 不是 -1，就会进 else
- `-1`是永久等待，其余的是超时等待


- `!is_set_flag_.load()` 如果是false就继续进入等待
- 如果是`true`就进入另外一个分支

- 弹出队列中储存的元素，而队列中储存的元素正是bool值


### 锁防止数据的竞争

下面三种函数，不同的线程之间可能会调用，造成这组队列的数据之间的竞争；因为访问这组队列数据的方法只有以下三种方法，因而下面三种方法加上同一把锁就好了

所有会动这个队列（或在其上做强一致读写的）路径，都用的是同一把 `queue_mutex_`：

|接口|对 `queue_mutex_` 的使用|
|---|---|
|`PushSignal`|`lock` → 可能 `pop`/`push` → `unlock`|
|`Size`|`lock` → 读 `size()` → `unlock`|
|`PopSignal`|开头快路径（59–67）、以及被唤醒后再取货（115–123）|

因此：多个线程无论调用 Push、Pop 还是 Size，只要碰到队列，都争用同一把 `queue_mutex_`，从而避免对 `event_queue_` 的并发读写造成数据竞争。  
这就是你说的“锁防止线程间数据竞争”在这里的具体体现：共享数据结构 = `event_queue_`，互斥量 = `queue_mutex_`



### 锁的封装

```cpp
std::unique_lock<std::mutex> lck(cond_mutex_);
```

- **`std::mutex`**
    
    C++ 标准**互斥锁**，作用：**同一时间只允许一个线程加锁**，防止多线程同时修改数据。
    
- **`std::unique_lock`**
    
    C++ 标准**锁管理工具**（不是锁本身！是锁的**管理员 / 包装器**）。
    
    它负责帮你自动上锁、解锁，不用手动写 `lock()`/`unlock()`。
    
- **`lck`**
    
    你定义的**锁管理对象**名字。
    
- **`(cond_mutex_)`**
    
    把 `cond_mutex_` 这个互斥锁交给 `unique_lock` 管理




# 📅2026-05-11

## 构造函数

**默认构造函数：**

```cpp
class A {
    int x;       // 基本类型
    string str;  // 类类型
public:
    // 编译器自动生成默认构造函数
};

A a; 
// 结果：
// str 被默认构造（空字符串）
// x 是随机垃圾值
```

- **调用父类的默认构造函数**（如果有继承）
- **调用成员对象的默认构造函数**（类类型成员）
- **不对基本数据类型（int/char/float/ 指针）做任何初始化**（它们会是随机垃圾值）


```c++
#include <iostream>
#include <string>
using namespace std;

class Person
{
private:
    string name;
    int age;
public:
    // 1. 默认构造
    Person();

    // 2. 有参构造
    Person(string n, int a);

    // 3. 拷贝构造
    Person(const Person& other);

    // 4. 移动构造 C++11
    Person(Person&& other);

    // 5. 转换构造（单参）
    explicit Person(int a);

    // 6. 委托构造
    Person(string n);

    // 打印
    void show() const;
};
```

- 不同的构造函数：没有返回值，但是传入不同类型的参数

### 各种构造函数的实现

**默认构造函数：**
```c++
// 无参，编译器默认合成就是空实现
Person::Person() : name(""), age(0) 
{
    cout << "默认构造\n";
}

Person p1;      // 调用默认构造
Person p2{};    // C++11 列表初始化，也调默认构造
```
- 简化的构造函数使用方法：直接在实例化对象旁边传参

**有参构造：**
```c++
Person::Person(string n, int a) : name(n), age(a)
{
    cout << "有参构造\n";
}

Person p3("张三", 20);
Person p4 = Person("李四", 18);
```
- 标准写法：左边创建对象，右边调用函数进行实例化


**拷贝构造：**
```c++
// 同类型常量引用
Person::Person(const Person& other) 
: name(other.name), age(other.age)
{
    cout << "拷贝构造\n";
}

Person p8 = p3;   // 拷贝构造
Person p9(p3);    // 拷贝构造
```
- 简化形式还是变量后面直接传参，但是标准形式不是调用函数；是不是将等号的作用重定义了？


**继承场景：**
```c++
class Student : public Person
{
private:
    int id;
public:
    // 子类构造 显式调用父类有参构造
    Student(string n, int a, int i) : Person(n, a), id(i)
    {
        cout << "子类构造\n";
    }
};

Student s("小明", 19, 1001);
```
- 子类构造函数书写时，必须首先初始化父类构造，否则默认父类的构造函数不需要传参


**移动构造：**

```c++
#include <iostream>
#include <string>
using namespace std;

class Person
{
private:
    string name;
    int age;
public:
    Person(string n, int a) : name(n), age(a)
    {
        cout << "有参构造\n";
    }

    // 拷贝构造
    Person(const Person& other) : name(other.name), age(other.age)
    {
        cout << "拷贝构造\n";
    }

    // 移动构造
    Person(Person&& other) : name(move(other.name)), age(other.age)
    {
        cout << "移动构造\n";
    }
};

int main()
{
    cout << "--- 左值赋值 ---\n";
    Person p1("小明", 20);
    Person p2 = p1;        // 走拷贝构造

    cout << "\n--- 临时右值 ---\n";
    Person p3 = Person("小红", 19); // 走移动构造
    return 0;
}
```

- 拷贝构造参数：`const Person&` 能接收左值、也能接收右值
- 移动构造参数：`Person&&` **只能接收右值**

当两个都能匹配时，C++ 重载决议：
右值优先选 && 版本（移动构造）--拷贝是复制，新旧各不影响；移动是抢夺资源，旧对象变空壳


## 析构函数

```c++
class A {
    int* p = new int;  // 指针
    string str;        // 类对象
public:
    // 编译器生成默认析构
};

A a;
// 析构时：
// str 被正确释放
// p 指向的堆内存 → 泄漏！！！
```
- 析构函数释放的内存是有限制的：
- 执行函数体（空）
- **调用成员对象的析构函数**
- **调用父类的析构函数**
- **绝对不会自动 delete 指针成员！**（这是很多内存泄漏的根源）


## 栈上创建对象与堆上创建对象

### 1. 栈上创建（普通定义）

```cpp
Person p;
Person p2("张三", 20);
```

- 内存在**栈**
- **自动调用构造函数**
- 出作用域**自动析构、自动释放内存**

### 2. `new` 创建（堆上）

```cpp
Person* p = new Person;
Person* p2 = new Person("李四", 18);
```

- 内存在**堆**
- `new` 做两件事：
    1. **申请堆内存**
    2. **自动调用对应构造函数**
- 不会自动释放，必须手动 `delete`




# 📅2026-05-12

## 禁止拷贝

```c++
// 禁止拷贝构造
HaierSdkNetwork(const HaierSdkNetwork&)            = delete;
// 禁止赋值重载
HaierSdkNetwork& operator=(const HaierSdkNetwork&) = delete;
```
- 有很多类的对象是禁止拷贝赋值的：
- 如果一个类中包含指针，直接拷贝，两个指针指向同一个地址，会引发一些灾难，比如重复内存释放；
- 有时候一个类只能使用一个对象进行管理，多个对象管理会造成时序问题


```c++
class SdkWrapper {
public:
    int* handle;

    SdkWrapper() {
        // 模拟申请SDK资源
        handle = new int(100);
    }
    ~SdkWrapper() {
        delete handle; // 释放资源
    }

    // 不写=delete，编译器默认生成浅拷贝
};

int main() {
    SdkWrapper a;
    SdkWrapper b = a;   // 浅拷贝，b.handle 和 a.handle 同地址

    // 出作用域：先析构a，再析构b
    // a 删了堆内存，b 再删一次 → 崩溃
    return 0;
}
```


## inline关键字

```c++
    inline void SetIsDuringNetConfig(bool status) { is_during_net_config = status; };
    inline bool GetIsDuringNetConfig() { return is_during_net_config; };
    inline void SetPlayWifiConfigVoiceFlag(bool flag) { has_play_wifi_config_voice_ = flag; };
    inline bool GetPlayWifiConfigVoiceFlag() { return has_play_wifi_config_voice_; };

    /**
     * @brief  设备连接状态
     * @param status
     */
    inline void SetCloudConnStatus(bool status) { connection_status_ = status; }

    /**
     * @brief 获取与涂鸦云的连接状态
     * @return true
     * @return false
     */
    inline bool GetCloudConnStatus() { return connection_status_; }
```
- 较短的代码直接在头文件中完成定义，这样可以直接展开在使用的地方
- 一般都是共有的成员函数操作私有的成员变量
- 在 C++ 里，写在类定义大括号里面的成员函数，默认就是内联函数（implicitly inline），不必再写 `inline`

## 名称替换

```c++
typedef Singleton<HaierSdkNetwork> sHaierSdkNetwork;
#define HAIER_SDK_NETWORK sHaierSdkNetwork::Instance()

#define IOT_SDK HAIER_SDK_NETWORK.iot_sdk_
```
- 两种替换方式，前者更为安全，后者更加简洁
- 单例类不需要实例化函数，直接使用类名调用函数

- 在C++中有一个与typedef等价的用法是using

## 函数构造对象

```c++
apos_iot_sdk::Apos_Iot_Init_Param_Func init_param() {
    apos_iot_sdk::Apos_Iot_Init_Param_Func env;
    memcpy(env.authentication_key, APOS_SDK_LICENSE, sizeof(APOS_SDK_LICENSE));
    std::string ip = "127.0.0.1";
    memcpy(env.server_ip, ip.c_str(), ip.size());
    return env;
}
```
- 创建一个局部对象实例
- 使用`memcpy`将字符串加载进对象
- 返回一个对象实例

## 常量引用、非常量引用、结构体

```c++
int SendCloudMap(const std::string &filePath, PushFileResult &result);
int SendBufferMap(const std::string &filePath, PushFileResult &result);
```
- 常量引用：一般作为输入值
- 非常量引用：一般作为输出值
- C++中的结构体：不需要使用struct，直接作为类名使用


## `promise`/`future`

**类似于生产者消费者**

![](Pasted%20image%2020260512140339.png)

时间 →

线程 A:  [ get_future ] [ wait........阻塞........ ] [ 被唤醒 ] [ get() 得到 T ]
                              ↑                              ↑
线程 B:       [ 干活................ ] [ set_value(T) ] -----┘
                                              │
                                    写入共享状态，标记 ready

```c++
// 回调函数
push_file_result_cb cb = [](uhsd_s32 errNo, uhsd_char *file_name_c, uhsd_char *file_index_c, uhsd_char *playUrl_c, uhsd_void *cb_param) {
	LOGD("uhsd_push_file_to_oss_blocking cb errNo:%d file:%s, index:%s", errNo, file_name_c, file_index_c);
	LOGD("uhsd_push_file_to_oss_blocking cb url:%s", playUrl_c);
	LOGD("uhsd_push_file_to_oss_blocking cb param:%p", cb_param);

	auto wrapper_ptr = static_cast<CallbackWrapper*>(cb_param);
	if (wrapper_ptr && *wrapper_ptr) {
		try {
			PushFileResult result;
			result.errNo = errNo;
			if (file_name_c)
				result.file_name = file_name_c;
			if (file_index_c)
				result.file_index = file_index_c;
			if (playUrl_c)
				result.playUrl = playUrl_c;

			(*wrapper_ptr)->set_value(result);
			delete wrapper_ptr;
		} catch (...) {
			// 避免异常传播
			LOGW("uhsd_push_file_to_oss invalid cb param");
		}
	} else {
		LOGW("uhsd_push_file_to_oss cb param invalid, interface run timeout");
	}
};
```
- 这里相当于生产者，在同一个函数中调用，结束上述逻辑中消费者的阻塞
- `static_cast<CallbackWrapper*>(cb_param)`将后续原始的指针转化为前面有特殊结构的指针，便于后续操作
## 智能指针

**可以将智能指针理解为对象+指针，有两种方法可以使用，含义不相同**

- `.`：智能指针这个对象自己的接口。
- `->` / `*`：透过智能指针，去操作它里面保存的那份「指向的对象」。


## 上传地图函数逻辑

```c++
auto* cb_param_ptr = new CallbackWrapper(promise_ptr);
```
- 拷贝方式在堆上创建共享指针对象



# 📅2026-05-15

## 海尔的SDK进行封装

**海尔的SDK是用来操作海尔云端数据的**


**回调函数的理解：**

```c++
int call_result = uhsd_file_push_del_remote_file(
	const_cast<uhsd_fs_del_file_info_t*>(&del_info),
	cb,
	cb_param_ptr
);
```
- 将回调函数`cb`传递给SDK，但不一定在此时调用
- 执行后面这一个函数，并接收结果

```text
T1:  [ 准备 promise/future ] [ uhsd_file_push_del_remote_file 登记 ] [ wait_for 阻塞…… ]
                                                                          ↑
T2:       [ SDK 内部删文件 / 网络 …… ] [ 调 cb(ret) ] [ set_value ] -------┘ 唤醒 T1
```
- 解决线程A的运行依赖于线程B的结果



## 初始化命令表

```c++
std::map<std::string, std::function<int(const std::string&)>> cmd_obj_api_table_;
```
- 创建一份键值对表格：键是字符串；值是函数，并且框定了函数的参数和返回值

```c++
cmd_obj_api_table_[HAIER_Y_CLEAN_MODE]           = std::bind(&TaskControl::Cmd_YMop, &TASK_CTL_INS, std::placeholders::_1);
```
- 命令表的使用方法：找到对应的键值所在的行->调用对应的值函数，并且传入参数






# 📅2026-07-01

## 单例模式

```bash
# 引用、地址、指针
返回引用 T&：只是原有堆对象的别名，无拷贝，共用唯一实例，推荐单例使用；
返回实例 T：会拷贝生成全新独立对象，破坏单例设计，绝对不能这么写；
指针只是存储地址，*指针 得到对象本体，根据函数返回值类型决定是返回别名还是拷贝副本
```


```cpp
template<typename T>
class Singleton
{
public:
	static T& Instance()
	{
		//pthread_once(&ponce_, &Singleton::init);
		if (NULL == value_)
		{
			value_ = new T();
		}
		return *value_;
	}

private:
	Singleton();
	~Singleton();

	Singleton(const Singleton&);
	Singleton& operator=(const Singleton&);

	static void init()
	{
		value_ = new T();
		//::atexit(destroy);
	}

	static void destroy()
	{
		delete value_;
	}

private:
	//static pthread_once_t ponce_;
	static T*             value_;
};

//template<typename T>
//pthread_once_t Singleton<T>::ponce_ = PTHREAD_ONCE_INIT;

template<typename T>
T* Singleton<T>::value_ = NULL;


/* 后续调用 */
typedef Singleton<SomeClass> sSomeClass;
#define SOME_INS sSomeClass::Instance()
```


类进行实例化，第一次资源进行消耗：
```cpp
WIFI_CONFIG_INS.Init();
// 展开为 Singleton<WifiManager>::Instance()
```

**资源消耗及周期：**

| 阶段                       | 消耗内容                      | 存储区域 (ROM/RAM)    | 发生时机                 | 是否持久                    |
| ------------------------ | ------------------------- | ----------------- | -------------------- | ----------------------- |
| 模板实例化代码、Instance () 等机器码 | 函数二进制指令                   | ROM（.text 段）      | 编译链接阶段               | 永久存在                    |
| 静态指针 value_              | 64 位系统 8 字节指针变量           | RAM（.data/.bss 段） | 程序启动初始化              | 进程整个生命周期有效              |
| new T () 创建堆对象           | sizeof (WifiManager) 大小内存 | RAM（堆 heap）       | 首次调用 Instance () 时分配 | 进程全程存在（现有逻辑无 delete 释放） |
| 对象内部成员（string、mutex 等）   | 成员自身占用内存                  | RAM（跟随堆对象）        | 对象构造函数执行时            | 与堆对象生命周期同步              |
1. 代码占 ROM：每个不同的 `T` 会生成一套 `Singleton<T>` 代码；`T` 越多，Flash 占用越多。
2. 指针占 RAM：每个 `T` 有一个 `value_`，从启动到结束一直占 RAM（极小）。
3. 对象占 RAM：首次 `Instance()` 时 `new` 在堆上分配，当前实现不会释放，等于常驻 RAM。
4. 懒加载：若某 `Singleton<T>` 从未被调用，则只有指针槽位 + ROM 代码，没有 `T` 对象的堆内存。





## 单例模式的使用原因

WiFi、地图、OTA、设备状态在物理上各只有一份，单例与“全局唯一资源”匹配



类定义-->使用单例模式中返回静态实例的函数-->调用实例相关函数






## 拉取另一代码分支

```bash
# 1. 获取远程更新
git fetch origin

# 2. 操作后，发现所有远程分支拉取到本地远程仓库
git branch -r

# 3. 创建并切换到指定远程分支
git switch feature_m7a_iot
```



# 📅2026-07-02

**一个类代表一个实际功能，将这个类注册一个单例模式实例**


```cpp
int HaierSdkNetwork::Init() {
    // 首先进行初始化
    apos_iot_sdk::Apos_Iot_Init_Param_Func param_func = init_param();
    iot_sdk_                                          = apos_iot_sdk::AposSdkIot::Instance();
    try {
        iot_sdk_->apos_iot_init(param_func);
        iot_sdk_->apos_iot_service_init();
    } catch (const std::system_error& e) {
        std::cerr << "Initial sdk: caught system_error with code " << e.code() << ": " << e.what() << '\n';
        // TODO. reinitial sdk
    }
    HAIER_PARAM_INS.Init(); //参数初始化
    HAIER_ADPATER.Init();    // APOS SDK 启动涂鸦协议适配器
    return 0;
}

}   // namespace HAIER_IOT
```
- 上述函数是整个IOT网络的起始入口
- `AposSdkIot`是第三方库（仅提供头文件，不提供对应函数实现）提供的类；乐动提供的底层通信框架（CallServer、事件订阅等）
- `HAIER_PARAM_INS`是适配层代码封装的一个类；使用乐动提供的API获得产品 License、SN、MAC、区域等产品信息
- `HAIER_ADPATER`也是本套代码封装的一个类；封装海尔提供的API，获得海尔云端协议（uhsd SDK）、命令表、各业务 Service

- 通过命名空间-类-函数具体索引，创建类的实例`iot_sdk_`，后续调用类的函数
- `apos_iot_init`、`apos_iot_service_init`具体函数的作用底层SDK仅提供头文件实现，具体的函数进行隐藏



## 重启IOT进程

```bash
1.机器烧录开调试固件，后续可以输入账号密码登录系统
2.开机后输入用户名
root
3.输入密码
autopack1688
4.进入指定目录输入指令
[root@AutoPack_Sweeper:~]# cd /tmp/approm
5.强制终止特定名称的进程
[root@AutoPack_Sweeper:/oem/bin/app]# killall autopack_manager
6.强制终止以特定名称开头的进程
[root@AutoPack_Sweeper:/oem/bin/app]# killall network*
7.在当前目录下启动名为 `network` 的可执行程序，并让它在后台运行
[root@AutoPack_Sweeper:/oem/bin/app]# ./network &
```


```bash
# 使用指令进行重启配网
cd /tmp/approm/
./event_tool -p -t "/event/keyboard" -e "network_config" -d "{}"
```


## 查找设备IP

```bash
for ($i=1;$i -le 254;$i++){ping -w 1 -n 1 10.38.27.$i | Out-Null}
arp -a
```




# 📅2026-07-03

## 分析乐动系统进程

```bash
[root@uplus-haier-vclrbt-3ba0:~]# procrank
warning: could not create process interface for 2854
  PID       Vss      Rss      Pss      Uss  cmdline
 1263   283092K   15968K    9995K    9100K  app_sweeper2
 1253   185312K   12512K    9054K    8596K  network
 1278    54224K   13228K    7803K    7076K  navigator_pl
 1216   378068K   11844K    6291K    5564K  sensor_node_carrier
 1252    99208K   11404K    5843K    5004K  apos_server
 1261   124292K   11524K    5495K    4652K  debug_proxy
 1265    40652K    9008K    3883K    3248K  slam_pose_provider_pl
 1262    54584K    8044K    3016K    2404K  sensor_node_point_laser
 3734     5612K    4640K    2026K    1740K  wpa_supplicant
 1151    19100K    6456K    1674K    1140K  ./autopack_manager
  787     4356K    2764K     619K     444K  /config/bin/play_daemon
 2523     2604K    1796K     332K     160K  /usr/sbin/dropbear
 1871     2604K    1772K     310K     140K  /usr/sbin/dropbear
 2524     2664K    1892K     290K     100K  -sh
 1155     2540K    1552K     254K      72K  /bin/sh
 2542     2536K    1508K     223K      72K  sh
  702     2252K    1504K     207K      56K  /usr/sbin/dropbear
 2857     1736K    1156K     192K     144K  procrank
    1     2536K    1520K     181K      76K  init
 2319     2536K    1508K     179K      72K  -/bin/login
 3858     2536K     816K     168K     124K  udhcpc
 2848     2404K    1004K     111K      56K  sleep
                           ------   ------  ------
                           58159K   50040K  TOTAL

RAM: 120800K total, 23000K free, 2548K buffers, 31788K cached, 18764K shmem, 11724K slab
```

遵循故障隔离、实时分级、内存隔离、单一职责标准分层拆分；高内存、高实时、易阻塞模块全部独立进程，轻量系统工具单独部署，搭配共享内存完成跨进程高频数据交互，适配本机 120MB 小内存硬件。

| 分层       | 进程名                                         | 划分依据                     | 核心收益                           |
| -------- | ------------------------------------------- | ------------------------ | ------------------------------ |
| 核心业务调度   | app_sweeper2                                | 整机清扫主逻辑，高优先级、内存占用最大，故障隔离 | 外围模块崩溃不中断本地清扫，独立监控内存泄漏         |
| 导航规划层    | navigator_pl                                | 算力密集路径算法，准实时业务           | 计算阻塞不干扰电机、传感器时序                |
| 定位建图层    | slam_pose_provider_pl、apos_server           | 高频位姿解算，地图存在内存累积风险        | 可单独重启重置地图，定位异常互不影响             |
| 传感器采集层   | sensor_node_carrier、sensor_node_point_laser | 按外设拆分，硬件 IO 易阻塞          | 单传感器故障仅自身异常，采样周期互不干扰，共享内存传原始数据 |
| 网络通信层    | network                                     | 存在网络 IO 阻塞、云端长连接         | 断网 / 云端无响应不影响扫地本地运行            |
| 辅助业务层    | debug_proxy、autopack_manager、play_daemon    | 日志、OTA、语音播报，低优先级 IO 操作   | 频繁打印、音频阻塞不抢占算法 CPU             |
| 系统原生轻量进程 | wpa_supplicant、dropbear、sh、udhcpc、init      | 系统基础工具，内存占用极低            | 仅提供调试 / 网络底层能力，故障不干涉业务         |


## CPU核心、线程、进程关系精简总结
### 1. 基础概念
1. **CPU物理核心**：硬件运算单元，真实并行计算载体；
2. **CPU逻辑线程（超线程）**：单核心时分复用，提升核心利用率，硬件最大并行任务数 = 总逻辑线程数；
3. **进程**：独立地址空间的程序容器，一个进程可包含多条执行线程；操作系统调度最小执行单元是**线程**，不是进程。

### 2. 数量关系对照表
| 数量关系               | 运行特征                           | 性能影响            | 嵌入式扫地机风险                 |
| ------------------ | ------------------------------ | --------------- | ------------------------ |
| 活跃线程总数 ≤ CPU逻辑线程数  | 无争抢，极少上下文切换                    | 性能最优、实时稳定       | 激光/IMU采样无丢帧，SLAM定位平稳     |
| 活跃线程略多于逻辑线程（1~2倍）  | 线程轮流分时调度，切换开销低                 | 轻微性能损耗，业务可正常运行  | 后台网络、日志短时抖动，不影响清扫核心      |
| 活跃线程远大于逻辑线程（3倍及以上） | 频繁上下文切换，大量CPU消耗在内核调度           | 算力浪费、延迟飙升、吞吐量下降 | 传感器丢数据、路径规划卡顿、电机控制时序错乱   |
| 进程数量过多（哪怕总线程不多）    | 每个进程独立页表、task_struct，IPC通信开销增加 | 内存占用升高、系统调度额外负担 | 小内存设备更容易内存紧张，模块间数据传输延迟变大 |

### 3. 核心规律
1. 决定整机并行上限的是**逻辑线程数**，不是物理核心，也不是进程数量；
2. 进程只做资源隔离，不直接参与并行运算，并行能力由进程内部线程决定；
3. 相同线程总量下：进程越少性能越好，进程越多调度与内存开销越大；
4. 架构取舍：多进程换取故障隔离，但要控制进程总数、控制内部活跃线程，避免超出CPU线程承载能力。

### 4. 项目落地结论
1. 传感器、SLAM、导航等高实时模块内部活跃线程，总数不超过CPU逻辑线程；
2. 网络、调试、shell等辅助进程降低调度优先级，减少对核心业务抢占；
3. 按需拆分进程，不无限拆分，平衡故障隔离与CPU、内存调度开销。








## 计算机网络相关概念

## 网关的概念

`本机IP & 子网掩码` 必须等于 `网关IP & 子网掩码`：网关格式与内网IP格式是统一的

```bash
ipconfig

   连接特定的 DNS 后缀 . . . . . . . :
   本地链接 IPv6 地址. . . . . . . . : fe80::8d00:3fc1:f81b:bbe4%23
   IPv4 地址 . . . . . . . . . . . . : 192.168.70.66
   子网掩码  . . . . . . . . . . . . : 255.255.255.0
   默认网关. . . . . . . . . . . . . : 192.168.70.254
```


**网络由多层独立局域网层层嵌套构成；每一层局域网仅有一个出口网关，网关设备同时具备两层身份：在上一级网络中是普通终端 IP，在下一级局域网中是全网出口网关 IP**



## 查看局域网下的其他设备IP

1. 先 ping 网段广播，缓存所有设备 ARP

```cmd
for /L %i in (1,1,254) do ping -w 1 -n 1 192.168.1.%i
```

> 把`192.168.1`换成你自己网关网段，查看本机网段：`ipconfig`，找 IPv4 地址前三段

2. 读取全部在线设备 IP+MAC

```cmd
arp -a
```



## NAT 路由 / WISP 中继、桥接 AP 中继（纯信号放大）
### 区别 1：是否新建独立子网、有无双层 NAT

1. **NAT 路由 / WISP 中继**
    
    自建新网段（主 192.168.1.0，副 192.168.2.0），双层 NAT；
    
    电脑 ipconfig 看到网关是副路由 IP。
2. **桥接 AP 中继（纯信号放大）**
    
    不新建网段，全设备同网段，**零额外 NAT**；
    
    电脑 ipconfig 网关永远是主路由 192.168.1.1。

### 区别 2：工作层级完全不同

- NAT/WISP：工作在**三层网络层**，要解析 IP 包、修改源 IP（NAT）、维护路由表；
- 桥接中继：只工作在**二层数据链路层**，只转发 WiFi 帧，不碰 IP 地址，完全透明。

### 区别 3：设备互通、端口转发能力天差地别

1. WISP/NAT 双层路由
    
    - 主路由设备**无法直接访问副路由下的电视、NAS、摄像头**；
    - 外网远程控制、游戏 UPnP、端口映射必须在**两级路由分别设置**，极易失败；
    - 设备隔离性强，适合访客网络、隔离智能家居。
    
2. AP 桥接中继
    
    - 全屋设备互相发现、投屏、文件共享无阻碍；
    - 只需要主路由做一次端口转发，无多层限制；
    - 适合家庭全屋覆盖、智能家居统一组网。




## 多种网卡

**访问网络，电脑中有多种网卡硬件，每一种都可以进行DHCP、静态IP等设置**

`win+R`输入`ncpa.cpl`





## 抓取APP日志、设备端日志

### 抓取设备端日志

```bash
1. USB转网口连接待测机器，为机器分配固定IP地址
2. 乐动有权限的上位机软件，找到对应IP地址的机器，发送开始调试指令
3. MobaXterm进入ssh上位机
   用户名：root
   密码：autopack1688
4. 进入特定目录，杀掉自动挂起其他进程的进程，否则重启进程会有冲突
   [root@AutoPack_Sweeper:~]# cd /tmp/approm
   [root@AutoPack_Sweeper:/oem/bin/app]# killall autopack_manager
5. 杀掉旧的IOT进程，并进行重启
   [root@AutoPack_Sweeper:/oem/bin/app]# killall network*
   [root@AutoPack_Sweeper:/oem/bin/app]# ./network
```
- 中间可以使用`procrank`查看进程情况
- 海尔印度机型点击配网按键之后，不会杀死 `network`进程，可以直接取相关日志
- 海尔泰国水洗机`./network`之后点击配网案件，会自动杀掉`network`进程，需要再次执行一下`network`进行取日志

```bash
# 使用指令进行重启配网
cd /tmp/approm/
./event_tool -p -t "/event/keyboard" -e "network_config" -d "{}"
```




### 安卓手机抓取app日志

进入开发者选项，开启USB调试
- 开启开发者选项：设置->关于手机->连续点击版本号->手机提示“您现在处于开发者模式”
- 搜索“开发者选项”：进入->开启USB调试

###### CMD配置ADB

```bash
1.以管理员身份打开CMD
2.安装自动安装ADB并配置环境的包管理器：
@"%SystemRoot%\System32\WindowsPowerShell\v1.0\powershell.exe" -NoProfile -InputFormat None -ExecutionPolicy Bypass -Command "iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))" && SET "PATH=%PATH%;%ALLUSERSPROFILE%\chocolatey\bin"
3.一键安装ADB
choco install adb -y
4.验证是否安装成功
adb version
5.输入指令、开启读取日志；`ctrl+C `结束读取日志
adb shell logcat > D:\app_log\HW2T_android.log
```

**手机需要全程亮屏，并在手机上操作允许USB调试




# 📅2026-07-06

## 云端->本地回调函数

```cpp
bool HaierAdapter::CallbackInit() {
    // 设置状态通知回调，设备配置、连云、绑定、连接路由器等状态均通过该状态回调通知.
    uhsd_s32 res = uhsd_dev_status_register(HaierStatusCallback, this);
    LOGW("uhsd_dev_status_register res : %d", res);

    // 设置【读】设备 通知回调，开发者只有设置该回调，才能收到设备读属性通知
    res = uhsd_dev_set_device_read_cb(HaierDeviceReadCallback);
    LOGW("uhsd_dev_set_device_read_cb res : %d", res);

    // 设置【写】设备 通知回调，开发者只有设置该回调，才能收到设备写属性通知
    res = uhsd_dev_set_write_cb(HaierWriteCallback);
    LOGW("uhsd_dev_set_write_cb res : %d", res);

    // 设置【操作】设备 通知回调，开发者只有设置该回调，才能收到设备op通知
    res = uhsd_dev_set_op_cb(HaierDeviceOperationCallback);
    LOGW("uhsd_dev_set_op_cb res : %d", res);

    /* 7. 设置【启动设备解除配对】通知回调，开发者只有设置该回调后，才能够收到云端推送的
     * 设备解除配对请求
     */
    res = uhsd_dev_set_start_depairing_notify_cb(HaierStartDepairingCallback, NULL);
    LOGW("uhsd_dev_set_start_depairing_notify_cb res : %d", res);

    /**
     * @brief 注册资源相关回调
     */
    res = uhsd_dev_register_rsc_related_cb(ResourceNotifyCallback, nullptr,   // 如果不需要回传参数，可以传入nullptr
                                           ChannelStatusCallback, nullptr     // 如果不需要回传参数，可以传入nullptr
    );
    LOGW("uhsd_dev_register_rsc_related_cb res : %d", res);

    return true;
}
```
- `res = uhsd_dev_set_write_cb(HaierWriteCallback);`开局注册一次，使得后续写指令知道调用哪一个函数


```cpp
int HaierAdapter::HaierWriteHandle(const std::string &cmd, const std::string &cmd_value) {
    std::lock_guard<std::mutex> locker(write_mutex_);
    if (cmd != HAIER_GET_SPECIAL_MAP) {
        auto iter = cmd_obj_api_table_.find(cmd);
        if (iter != cmd_obj_api_table_.end()) {
            return iter->second(cmd_value);
        } else {
            LOGD("HaierWriteHandle cmd:%s not found in cmd_obj_api_table", cmd.c_str());
        }
    }
    return 0;
}
```
- 根据传入的参数，决定返回哪一个函数指针



```cpp
uhsd_s32 HaierAdapter::HaierWriteCallback(uhsd_devHandle devHandle, uhsd_s32 req_sn, const uhsd_char *property_name, const uhsd_char *property_value, const uhsd_char *traceId) {
    std::ostringstream oss;
    oss << "Callback Info - req_sn: " << req_sn;
    if (property_name != nullptr) {
        oss << ", property_name: " << property_name;
    } else {
        oss << ", property_name: nullptr";
    }
    LOGD("HaierWriteCallback %s", oss.str().c_str());
    if (property_name && strcmp(property_name, HAIER_GET_SPECIAL_MAP) != 0) {
        LOGD("Property Name: %s Property Value: %s", property_name, property_value ? property_value : "NULL");
    }
    int result = HAIER_ADPATER.HaierWriteHandle(std::string(property_name), std::string(property_value));
    return uhsd_dev_write_resp(devHandle, req_sn, result, 0, traceId);
}
```
- 将要调用的函数指针、sn以及其余相关参数，传入API进行相关操作


- 上述的要调用的函数在初始化命令表中，下面拿初始化命令表中的一个函数举例子



## 上报设备状态

```cpp
int HaierAdapter::SendStausReport(uhsd_dev_pair_t pairs[], uhsd_s32 len) {
    // 上报设备状态
    int result = uhsd_dev_status_report(gw_devHandle, pairs, len);

    if (result == 0) {
        static bool s_send_status_ok_logged = false;
        if (!s_send_status_ok_logged) {
            LOGD("SendStausReport success result:%d", result);
            s_send_status_ok_logged = true;
        }
    } else {
        LOGE("SendStausReport failed result:%d", result);
    }
    return result;
}
```
- 调用SDK，将状态上报到海尔云端



## 将指令下发到机器

```cpp
class AposSdkIot {
 private:
   AposSdkIot* apos_iot_sdk_;
   ServiceManager* service_manager_;
   EventManager* event_manager_;
   Apos_Sdk_Handler handler_;
   Apos_Iot_Init_Param_Func init_param_func_;

   // PropertyManagerProtocol* property_;
 public:
    //C11后的特性，实现单列模式
    static AposSdkIot* Instance();
    OperateCode apos_iot_init(Apos_Iot_Init_Param_Func init_param_func);
    OperateCode apos_iot_service_init();
    OperateCode apos_iot_start();
    OperateCode apos_iot_stop();
 public:
    OperateCode CallServer(const std::string &service, const apos_serialize::IMessage *req, apos_serialize::IMessage *res);
    
    OperateCode CallServer(const std::string &service);    
    
    void Subscriber(const char* event, EventWorkerInnerPtr worker_ptr);

    static void apos_sdk_event_handle(int32_t handle, const char* event_id, int32_t msg_id, BinPack* in_msg);
    //临时提供给test_demo用于内部读取接口测试
    int test();
    
    //获取apos sdk的访问句柄
    int32_t GetAposHandler() { return handler_; };
 private:
    AposSdkIot();
};
```
- 上述是乐动提供的与机器通讯的类


- 主动做事：`CallServer`（读数据、控任务、设属性）
```cpp
// ① 纯命令（最常见）—— 控任务、重置耗材
CallServer(SRV_TASK_CONTROL_STOP_CLEAN, nullptr, nullptr);

// ② 只读 —— 查询数据
CallServer(SRV_MAP_GET_MAP_ID, nullptr, &srv.response);

// ③ 读写 —— 带参数的请求
CallServer(SRV_MAP_MERGE_AUTO_AREA, &req, &res);
```

- 被动响应：`Subscriber`（按键、状态变化、地图更新等）
- 在模块 `Init()` 时注册一次（如 `fault_event.cpp` 里的 `FaultEvent::Init()`）
- 之后不主动调用，等 APOS/机器侧事件发生再回调
- 典型：按键、故障、地图变更、清扫记录、OTA 进度等






# 📅2026-07-07

**这个项目本质上是一个协议桥接/联网模块：一头连扫地机本体系统（乐动 APOS），一头连海尔 IoT 云平台（OSDK/UHSD）**

![](Pasted%20image%2020260707002914.png)


## 日志格式及关键词

**格式 1：本工程业务日志（最重要，可直接定位代码）**

```bash
[日期 时间.毫秒] [主进程PID-线程TID/标签] [级别/文件名:行号] [消息内容]

2026/7/3 18:33:29.276 1873-1873/apos_iot_sdk D/main.cpp:39 TUYA SDK Demo start run ...
```
- 打印中有这个文件所在的代码文件以及代码行号


在`haier_adapter.cpp`设置OSDK的日志等级：
```cpp
uhsd_s32 level = UHSD_LOG_OFF;
uhsd_log_set(NULL, level);   // 日志等级
    
#define UHSD_LOG_OFF           0
#define UHSD_LOG_LEVEL_VERBOSE 1
#define UHSD_LOG_LEVEL_DEBUG   2
#define UHSD_LOG_LEVEL_INFO    3
#define UHSD_LOG_LEVEL_WARN    4
#define UHSD_LOG_LEVEL_ERROR   5
#define UHSD_LOG_LEVEL_FATAL   6
```

OFF=0（关）
  ↓ 开启后
Verbose=1 → Debug=2 → Info=3 → Warn=4 → Error=5 → Fatal=6
  最啰嗦 ────────────────────────────────► 最严重




**格式 2：海尔 OSDK 内部日志（SDK/网络层）:**

```bash
[OSDK-版本][级别][时间 t:线程名_线程ID s:序号] 模块 (函数名) 消息

[OSDK-5.6.0][E][2026-07-03 18:33:31:376 t:misc_1918 s:508] iwimp (wpactrl_connect) wpactrl open failed, 2.
```
- 在海尔提供的预编译库里，本仓库一般修改不了



**格式 3：LDRPC / APOS 底层 RPC 日志：**

```bash
[2026/7/3 18:33:29.275 F][apos_client.cpp:257] <<LDRPC>> AposSdkWrite AposClient not valid
```
- 乐动提供的SDK与底层机器之间建立联系



> [!NOTE] 专有名词
> 1. OSDK = 海尔 UHSD（UHome SmartDevice）SDK，也就是 `third_party/haier_sdk` 那一套，头文件以 `uhsd_` 开头
> 2. APOS = 乐动机器人的机器人操作系统/通信框架
> 3. RPC（Remote Procedure Call，远程过程调用）：像调本地函数一样，去调用另一进程/另一台机器上的函数
> 	- Push 端口 8801：联网进程向机器人发请求（读/写属性、调 Service）
> 	- Sub 端口 8802：订阅机器人事件（清扫开始、故障、配网等




## 根据日志分析程序运行流程


```bash
2026/7/3 18:33:29.870 1873-1873/apos_iot_sdk D/apos_iot_sdk.cpp:224 -------------apos_iot_service_init------------
```
- 这个日志格式与本工程打印格式相同，都是使用`LOGD`等C++函数进行打印
- 但是因为`apos_iot_sdk.cpp`是看不到的，提供的是链接库，而不是源码
- 能进行上述打印是因为引用了`apos_iot_sdk.h`，并且使用了文件中声明`apos_iot_sdk.cpp`中定义的函数，如`CallServer`、`Subscriber`等





- `main.cpp`
```cpp
int main(int argc, char *argv[]) {
    LOGD("TUYA SDK Demo start run ... ");
    if (HAIER_IOT::HAIER_SDK_NETWORK.Init() != 0) {
        LOGE("TuyaSdkNetwork Init failed.");
    }

    while (1) {
        std::this_thread::sleep_for(std::chrono::milliseconds(1000 * 10));
    }
    return 0;
}
```
- `LOGD("TUYA SDK Demo start run ... ")`作为一个标志，`HAIER_IOT::HAIER_SDK_NETWORK.Init()`函数调用


- `haier_sdk_network.cpp`
```cpp
#define APOS_SDK_LICENSE                                                                                   \
    "NWd56lK0nyxFfxmEvDNthpfabDcUN6htue0tJehmJNx4brgcxNGW/MA+0rVnCeyRTF2qtJ0oasTjQSozCqRUJUldfvQ4GG46U3t/" \
    "OD0zlAjeOT1VTJKzBtce6Rs+pAY7ielOahX9do70bACAZ7DJbk+BYg7n2sb/kFP/LsbiLt8="

apos_iot_sdk::Apos_Iot_Init_Param_Func init_param() {
    apos_iot_sdk::Apos_Iot_Init_Param_Func env;
    memcpy(env.authentication_key, APOS_SDK_LICENSE, sizeof(APOS_SDK_LICENSE));
    std::string ip = "127.0.0.1";
    memcpy(env.server_ip, ip.c_str(), ip.size());
    return env;
}
```
- 构造鉴权地址和通信地址，组合成一个变量，传入后续API


- `haier_sdk_network.cpp`
```cpp
int HaierSdkNetwork::Init() {
    // 首先进行初始化
    apos_iot_sdk::Apos_Iot_Init_Param_Func param_func = init_param();
    iot_sdk_                                          = apos_iot_sdk::AposSdkIot::Instance();
    try {
        iot_sdk_->apos_iot_init(param_func);
        iot_sdk_->apos_iot_service_init();
    } catch (const std::system_error& e) {
        std::cerr << "Initial sdk: caught system_error with code " << e.code() << ": " << e.what() << '\n';
        // TODO. reinitial sdk
    }
    HAIER_PARAM_INS.Init(); //参数初始化
    HAIER_ADPATER.Init();    // APOS SDK 启动涂鸦协议适配器
    return 0;
}
```
- `iot_sdk_->apos_iot_init(param_func)`调用乐动的SDK打印日志：连接本机 LDRPC（127.0.0.1:8801/8802），订阅 142 个机器人事件（清扫、WiFi、按键等）
- `iot_sdk_->apos_iot_service_init()`调用乐动的SDK打印日志：APOS IoT 服务层初始化



- `haier_adapter.cpp`
```cpp
bool HaierAdapter::InitOsdk() {
    // 初始化 SDK
    LOGD("Initializing OSDK...");

    // 判断区域：国内 / 海外
    if (HAIER_PARAM_INS.GetRegion() == 1) {
        region = UHSD_REGION_SEA;
    } else if (HAIER_PARAM_INS.GetRegion() == 0) {
        region = UHSD_REGION_DOMESTIC;
    } else {
        // License 未明确指定 region，通过 upCodeT 兜底判断
        if ((HAIER_PARAM_INS.GetProductUpcodet() == "F55241E00") || (HAIER_PARAM_INS.GetProductUpcodet() == "F55240E00") || (HAIER_PARAM_INS.GetProductUpcodet() == "F5524NE00") || (HAIER_PARAM_INS.GetProductUpcodet() == "lsuBs2p3Pi4")) {
            region = UHSD_REGION_SEA;
        } else {
            region = UHSD_REGION_DOMESTIC;
        }
    }
    LOGD("region %d", region);

    // OSDK 5.5+ 要求：海外版本必须在 uhsd_init 前调用 uhsd_set_domain_region_ext
    if (region == UHSD_REGION_SEA) {
        std::string country = HAIER_PARAM_INS.GetCountryCode();
        if (country.empty()) {
            country = "IN";   // 默认印度
        }
        uhsd_s32 ext_res = uhsd_set_domain_region_ext(const_cast<uhsd_char *>(country.c_str()), 0, 0);
        LOGD("uhsd_set_domain_region_ext country:%s, res:%d", country.c_str(), ext_res);
    }

    uhsd_sysinfo_cfg_t sysinfo_cfg;
    memset(&sysinfo_cfg, 0, sizeof(sysinfo_cfg));
    strncpy(sysinfo_cfg.file_path, "/data/cfg/", sizeof(sysinfo_cfg.file_path) - 1);
    strncpy(sysinfo_cfg.eth, "wlan0", sizeof(sysinfo_cfg.eth) - 1);
    uhsd_linux_syscfg_init(sysinfo_cfg);

    int res = uhsd_init();
    if (res) {
        LOGE("OSDK init failed");
        return false;
    }
    // 打印OSDK版本号
    uhsd_s8 *osdk_ver = uhsd_version();
    if (osdk_ver != NULL) {
        LOGI("OSDK Version: %s", osdk_ver);
    } else {
        LOGW("OSDK Version: unknown");
    }
    // uhsd_s32 level = UHSD_LOG_OFF;
    uhsd_s32 level = UHSD_LOG_OFF;
    uhsd_log_set(NULL, level);   // 日志等级
    LOGD("Haier log set to %d", level);

    return true;
}
```
- `HAIER_PARAM_INS.GetRegion()`与机器内部通讯，调用机器内部储存的变量作为判断依据--`IOT_SDK->CallServer(SRV_SYSTEM_GET_BASE_STATION_VERSION, &srv.request, &srv.response)`

- `LOGD("region %d", region);`上述函数会进行打印国家地区（0代表国内，1代表国外）
- `LOGD("uhsd_set_domain_region_ext country:%s, res:%d", country.c_str(), ext_res);`如果是国外，还可以打印机器内储存的国家地区

- 将机器中的配置、日志、网卡信息传入OSDK，再进行初始化、打印版本号、设置OSDK的日志等级



- `haier_adapter.cpp`
```cpp
bool HaierAdapter::CallbackInit() {
    // 设置状态通知回调，设备配置、连云、绑定、连接路由器等状态均通过该状态回调通知.
    uhsd_s32 res = uhsd_dev_status_register(HaierStatusCallback, this);
    LOGW("uhsd_dev_status_register res : %d", res);

    // 设置【读】设备 通知回调，开发者只有设置该回调，才能收到设备读属性通知
    res = uhsd_dev_set_device_read_cb(HaierDeviceReadCallback);
    LOGW("uhsd_dev_set_device_read_cb res : %d", res);

    // 设置【写】设备 通知回调，开发者只有设置该回调，才能收到设备写属性通知
    res = uhsd_dev_set_write_cb(HaierWriteCallback);
    LOGW("uhsd_dev_set_write_cb res : %d", res);

    // 设置【操作】设备 通知回调，开发者只有设置该回调，才能收到设备op通知
    res = uhsd_dev_set_op_cb(HaierDeviceOperationCallback);
    LOGW("uhsd_dev_set_op_cb res : %d", res);

    /* 7. 设置【启动设备解除配对】通知回调，开发者只有设置该回调后，才能够收到云端推送的
     * 设备解除配对请求
     */
    res = uhsd_dev_set_start_depairing_notify_cb(HaierStartDepairingCallback, NULL);
    LOGW("uhsd_dev_set_start_depairing_notify_cb res : %d", res);

    /**
     * @brief 注册资源相关回调
     */
    res = uhsd_dev_register_rsc_related_cb(ResourceNotifyCallback, nullptr,   // 如果不需要回传参数，可以传入nullptr
                                           ChannelStatusCallback, nullptr     // 如果不需要回传参数，可以传入nullptr
    );
    LOGW("uhsd_dev_register_rsc_related_cb res : %d", res);

    return true;
}
```
注册之后，对应的事件（状态）发生就会调用用户的函数




- `haier_adapter.cpp`
```cpp
bool HaierAdapter::InitAndSetDevHandle() {
    // HaierParameter::Init 较早执行时 License 可能尚未就绪，此处再拉取一次以保证 upCodeT 与区域判断一致
    HAIER_PARAM_INS.InitOtherInfo();

    // 关闭httpDNS域名解析功能，并进入验收环境，专用于印度机型验收
    // int ret_httpDNS = uhsd_dev_httpDNS_enable_set(0);
    // if (ret_httpDNS != 0) {
    //     LOGE("close httpDNS failed!");
    // }

    // 添加设备为普通设备
    gw_devHandle = uhsd_dev_new_devHandle(UHSD_DEV_ROLE_GENERAL);
    if (gw_devHandle < 0) {
        LOGE("Init device handle failed!");
        return false;
    }

    // 设置设备信息
    // (1) 设置typeId
    const uhsd_char *typeID = HAIER_PARAM_INS.GetProductTypeid().c_str();
    int res                 = uhsd_dev_set_devHandle_typeId(gw_devHandle, typeID);
    if (res < 0) {
        LOGE("Set devHandle typeId failed, res : %d", res);
        return false;
    }

    // (2) 设置devKey
    const uhsd_char *devKey = HAIER_PARAM_INS.GetProductDevkey().c_str();
    res                     = uhsd_dev_set_devHandle_devKey(gw_devHandle, devKey);
    if (res < 0) {
        LOGE("Set devHandle devKey failed, res : %d", res);
        return false;
    }

    // (3) 设置upCodeT
    const uhsd_char *upCodeT = HAIER_PARAM_INS.GetProductUpcodet().c_str();
    res                      = uhsd_dev_set_devHandle_upCodeT(gw_devHandle, upCodeT);
    if (res < 0) {
        LOGE("Set devHandle upCodeT failed, res : %d", res);
        return false;
    }

    // (5) 设置整机升级信息
    const uhsd_char *upFwType = upCodeT;
    const uhsd_char *upVer    = HAIER_PARAM_INS.GetProductVersion().c_str();
    res                       = uhsd_dev_set_devHandle_fotaInfo(gw_devHandle, upFwType, upVer);
    if (res < 0) {
        LOGE("Set devHandle fotaInfo failed, res : %d", res);
    }

    // (6) 设置设备名称
    const uhsd_char *dName = "U-VCLRBT";
    res                    = uhsd_dev_set_devHandle_hotspotName(gw_devHandle, dName);
    if (res < 0) {
        LOGE("Set devHandle dName failed, res: %d", res);
        return false;
    }

    // (7) 设置设备mac -- 该参数必须设置
    const uhsd_char *devMac = HAIER_PARAM_INS.GetMacAddr().c_str();
    res                     = uhsd_dev_set_devHandle_dMac(gw_devHandle, devMac);
    if (res < 0) {
        LOGE("Set devHandle dMac failed, res: %d", res);
        return false;
    }

    // (8) 添加module
    const uhsd_char *mac    = devMac;
    const uhsd_char *hwType = "ssc9211d";
    const uhsd_char *swVers = "1.0.0";
    gw_moduleHandle         = uhsd_dev_new_wifinetcard_module(mac, hwType, swVers);
    if (gw_moduleHandle < 0) {
        LOGE("Create wifinetcard module failed, res : %d", gw_moduleHandle);
        return false;
    }

    // (9) 添加module到设备
    res = uhsd_dev_add_module_to_device(gw_devHandle, gw_moduleHandle);
    if (res < 0) {
        LOGE("Add module to device failed, res : %d", res);
        return false;
    }

    appId  = "MB-ISPSDJDTSJDY-0000";
    appKey = "e4df57077dfea2f74806c556f3bcc8b4";
    // clientId = "";
    apiVersion = "v1";
    appVersion = "1.0.0";
    if (region == UHSD_REGION_DOMESTIC) {
        language = "zh_CN";
    } else if (region == UHSD_REGION_SEA) {
        language = "en_IN";
    }
    trace = "sweeper_data_report";

    LOGD("Init device handle success.");
    return true;
}
```
- 将已经声明的变量`gw_devHandle`初始化为一个普通设备，作为句柄，结合设备中保存的参数，一并传入后续SDK的API




# 📅2026-07-10

## IOT编译环境部署

- 查看所有用户的~目录配置
```bash
root@wangbohan:~# cat /etc/passwd
root:x:0:0:root:/root:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync
games:x:5:60:games:/usr/games:/usr/sbin/nologin
man:x:6:12:man:/var/cache/man:/usr/sbin/nologin
lp:x:7:7:lp:/var/spool/lpd:/usr/sbin/nologin
mail:x:8:8:mail:/var/mail:/usr/sbin/nologin
news:x:9:9:news:/var/spool/news:/usr/sbin/nologin
uucp:x:10:10:uucp:/var/spool/uucp:/usr/sbin/nologin
proxy:x:13:13:proxy:/bin:/usr/sbin/nologin
www-data:x:33:33:www-data:/var/www:/usr/sbin/nologin
backup:x:34:34:backup:/var/backups:/usr/sbin/nologin
list:x:38:38:Mailing List Manager:/var/list:/usr/sbin/nologin
irc:x:39:39:ircd:/var/run/ircd:/usr/sbin/nologin
gnats:x:41:41:Gnats Bug-Reporting System (admin):/var/lib/gnats:/usr/sbin/nologin
nobody:x:65534:65534:nobody:/nonexistent:/usr/sbin/nologin
systemd-network:x:100:102:systemd Network Management,,,:/run/systemd/netif:/usr/sbin/nologin
systemd-resolve:x:101:103:systemd Resolver,,,:/run/systemd/resolve:/usr/sbin/nologin
syslog:x:102:106::/home/syslog:/usr/sbin/nologin
messagebus:x:103:107::/nonexistent:/usr/sbin/nologin
_apt:x:104:65534::/nonexistent:/usr/sbin/nologin
lxd:x:105:65534::/var/lib/lxd/:/bin/false
uuidd:x:106:110::/run/uuidd:/usr/sbin/nologin
dnsmasq:x:107:65534:dnsmasq,,,:/var/lib/misc:/usr/sbin/nologin
landscape:x:108:112::/var/lib/landscape:/usr/sbin/nologin
sshd:x:109:65534::/run/sshd:/usr/sbin/nologin
pollinate:x:110:1::/var/cache/pollinate:/bin/false
wangbohan808:x:1000:1000:,,,:/home/wangbohan808:/bin/bash


root@wangbohan:~# grep "/bin/bash" /etc/passwd
root:x:0:0:root:/root:/bin/bash
wangbohan808:x:1000:1000:,,,:/home/wangbohan808:/bin/bash
```



- 切换用户
```bash
wangbohan808@wangbohan:~$ sudo -i
root@wangbohan:~# su - wangbohan808
wangbohan808@wangbohan:~$
```



- 安装WSL2的Ubuntu-18.04

- 创建统一的软链接，使得使用这个统一的软链接名称可以指向不同的用户名的主目录，保证编译的SDK路径一致
```bash
root@wangbohan:/home# ls
wangbohan808
root@wangbohan:/home# ln -s wangbohan808 peter
root@wangbohan:/home# ls
peter  wangbohan808

wangbohan808@wangbohan:~$ ls
test
wangbohan808@wangbohan:~$ cd /home/peter
wangbohan808@wangbohan:/home/peter$ ls
test
wangbohan808@wangbohan:/home/peter$ cd /home
wangbohan808@wangbohan:/home$ ls
peter  wangbohan808
```


- 安装gcc工具
```bash
sudo apt-get install build-essential
```

- 复制sdk到/home/wangbohan808 目录
```bash
wangbohan808@wangbohan:~$ cd /mnt/f/Resource/Linux/Haier_IOT
wangbohan808@wangbohan:/mnt/f/Resource/Linux/Haier_IOT$ cp ssd222d_sdk.tar.gz ~
wangbohan808@wangbohan:/mnt/f/Resource/Linux/Haier_IOT$ cd ~
wangbohan808@wangbohan:~$ tar -zxvf ssd222d_sdk.tar.gz
```

- 克隆代码
```bash
git clone https://gitlab.ldrobot.com/ldrobot/autopack/network_haier_normal.git

celink-dev-wangbohan
```

- 切换分支
```bash
# 更新远程分支列表，同步远端所有分支名称
git fetch origin
# 查看所有远程分支，确认目标分支名
git branch -r

git checkout -b dev origin/dev
# 语法：git checkout -b 本地分支名 远程仓库/远程分支名
# 创建并切换本地dev分支，关联远程dev
git switch -c dev origin/dev

# 旧版git
git checkout dev
# 新版git
git switch dev
```


- 查看当前ubuntu版本
```bash
wangbohan808@wangbohan:~$ lsb_release -a
No LSB modules are available.
Distributor ID: Ubuntu
Description:    Ubuntu 18.04.2 LTS
Release:        18.04
Codename:       bionic
wangbohan808@wangbohan:~$
```

- 库名进行软连接，以防潜在的风险
```bash
sudo ln -s /usr/lib/x86_64-linux-gnu/libmpfr.so.6 /usr/lib/x86_64-linux-gnu/libmpfr.so.4
```



## CMake文件解读


- 基础声明
```bash
cmake_minimum_required(VERSION 3.10)
project(network_haier_normal)

message("Process Project: ${CMAKE_CURRENT_SOURCE_DIR}")

SET(APPLICATION "network_haier_normal")
```

- `message`在使用cmake进行编译的时候会进行输出

```bash
wangbohan808@wangbohan:~/network_haier_normal/build$ cmake --version
cmake version 3.10.2

CMake suite maintained and supported by Kitware (kitware.com/cmake).
```
- 现有的CMake版本刚好>=要求的CMake版本


- 方案选择，这套代码似乎没有使用这一个变量

```bash
IF(NOT TUYA_T4_SOLUTION_SELECTION) 
	set(TUYA_T4_SOLUTION_SELECTION "public")
ENDIF()
```



- 指定格式可以调整构建时的代码大小
```bash
# 未指定时默认 Release，便于压缩体积；调试时使用 -DCMAKE_BUILD_TYPE=Debug
if(NOT CMAKE_BUILD_TYPE)
	set(CMAKE_BUILD_TYPE "Release")
endif()
```


- 设置变量名称，指定后续链接库的路径
```bash
set(LIB_DIR_NAME "ssd222d")
```


```bash
if(NOT DEFINED APOS_IOT_SDK_ROOT OR APOS_IOT_SDK_ROOT STREQUAL "")
	set(APOS_IOT_SDK_ROOT "${CMAKE_CURRENT_SOURCE_DIR}/../apos_iot_sdk")
endif()
set(APOS_IOT_SDK_ROOT "${APOS_IOT_SDK_ROOT}" CACHE PATH "Path to apos_iot_sdk project")
```
- 设置乐动使用的源码的路径，后续进行编译，并将编译产物放入第三方库
- 定义缓存变量 `APOS_IOT_SDK_ROOT`，类型为目录路径，说明文字为 `Path to apos_iot_sdk project`； 变量默认值复用当前已存在的 `APOS_IOT_SDK_ROOT` 值，并存入缓存持久化--跨构建保存


```bash
if(NOT DEFINED APOS_SDK_INSTALL_DIR OR APOS_SDK_INSTALL_DIR STREQUAL "" OR APOS_SDK_INSTALL_DIR MATCHES "/mnt/d/haier/sdk")
	set(APOS_SDK_INSTALL_DIR "${CMAKE_CURRENT_SOURCE_DIR}/../sdk")
endif()
set(APOS_SDK_INSTALL_DIR "${APOS_SDK_INSTALL_DIR}" CACHE PATH "apos_iot_sdk install prefix (make install output)")
```
- 可能使用wsl2已经部署的默认的sdk的情况，而不是使用乐动源码进行编译

- 文件进行编译与同步，将sdk编译产物后续移动到项目代码中的第三方库进行编译
```bash
set(APOS_SDK_LIB_SOURCE "${APOS_SDK_INSTALL_DIR}/lib/libapos_iot_sdk.so")
set(APOS_SDK_LIB_DEST   "${CMAKE_CURRENT_SOURCE_DIR}/third_party/apos_iot_sdk/lib/${LIB_DIR_NAME}/libapos_iot_sdk.so")
```




# 📅2026-07-16

## 整套IOT代码流程

![](Pasted%20image%2020260716140449.png)




# 📅2026-07-17

## 处理海尔OSDK

1. 编译Linux适配层
```bash
注意linux适配层不再编进OSDK库里。单独提供源代码 随SDK一起提供 在tag包里。
使用方法：改一下linux目录里面的编译器路径，直接在源码目录一下 执行make
即可直接生成库
```
- 使用Linux适配层源码进行编译的时候，同时打包出静态库（`.a`）与动态库（`.so`），两者在内容上一直，在使用时二选一
- 本项目使用静态库（`.a`）编译IOT源码

编译之前需要改变目录权限，或者直接而进行加权处理

```bash
# 用 root（或 sudo）
sudo chown -R wangbohan808:wangbohan808 ~/sources/OSDK5.6.0-RC2_ssc9211d_sea_MQTT_softap_ble_test_20260618/OSDK5.6.0-RC2_ssc9211d_sea_MQTT_softap_ble_test_20260618/src/linux/output

# 或者直接删掉，再以 wangbohan808 重新 make
sudo rm -rf ~/sources/OSDK5.6.0-RC2_ssc9211d_sea_MQTT_softap_ble_test_20260618/OSDK5.6.0-RC2_ssc9211d_sea_MQTT_softap_ble_test_20260618/src/linux/output
```

sudo make

2. 将编译后的内容以及原本OSDK中的内容统一打包到IOT代码仓库（头文件和代码编译后的文件分别放到对应位置）
![](Pasted%20image%2020260717090610.png)

3. IOT代码的编译技巧
```bash
李新编写了一个 make.sh文件，可以直接进行执行编译

在build目录执行 make install 可以将需要替换的系统文件直接进行打包到iot目录
```

4. 替换文件到系统后需要进行加权

